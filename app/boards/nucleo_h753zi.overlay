/* Add the red user LED (LD3, PB14) as a GPIO-controlled LED.
 * The in-tree DTS only exposes it as pwm-led0; the app needs led2. */

/ {
	leds {
		compatible = "gpio-leds";

		red_led: led_2 {
			gpios = <&gpiob 14 GPIO_ACTIVE_HIGH>;
			label = "User LD3";
		};
	};

	aliases {
		led2 = &red_led;
	};
};
